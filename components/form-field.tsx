import { FC } from "react";
import { Text, View } from "react-native";

export const FormField: FC = (props) => {
  return (
    <View className={"space-y-2"}>
      <Text className={"text-base text-gray-100 font-psemibold"}> </Text>
    </View>
  );
};
